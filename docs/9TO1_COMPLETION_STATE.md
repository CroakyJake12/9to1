# 9to1 Programme Completion State

Updated: 2026-09-23

## Programme status

- **Primary phase:** Phase 01 — Boards (in progress).
- **Current workstream:** preserve the existing Boards contract/HUI work while repairing the CUI app surface, shared themes, and remaining local validation.
- **Overall programme:** active; Phases 02–17 have not been promoted or claimed complete.
- **Canonical checkout:** `C:\Github\9to1` (explicit user correction; OneDrive is a preserved preparation duplicate).
- **Branch / HEAD:** `main` at `d949fcf23f10b75694d4b6614108c0d92eada9c4` after an external concurrent commit during reconciliation; preserve this commit and both checkouts.

## Completed in the current run

- Reconciled Boards work from audited upstream source while preserving the existing dirty checkout. No branch switch, merge, reset, or commit was performed.
- Changed the document editor to fill the space beside navigation. Removed the centered fixed-width paper card, outside margins, border, and rounded corners. The content keeps the original inner spacing as a fixed 40 px horizontal and 32 px vertical inset; page-level ink stays over the seamless editor surface.
- Reconnected Boards Light/Dark appearances to the shared CUI Boards Glow palette and applied its semantic/gradient resources to the shell and generated controls.
- Corrected the shared CUI loader so `grid-column` and `grid-row` remain attached coordinates even when applied to a `Grid` element. Added a nested-grid regression test.
- Corrected the loaded-board status label so a local board does not appear unsaved immediately after opening.

## Evidence

### Tests and gates

- Release Boards app suite: **PASS, 40/40**. This build includes the Boards app and covers the CUI loader, editor layout, Glow resources, theme appearances, and status mapping.
- Release CUI Runtime suite: **PASS, 52/52**, including the nested Grid attached-coordinate regression.
- Boards foundation static gate: **PASS** (`9to1 Workspace\Boards\tests\verify-boards-foundation.ps1`).
- Boards contract Release suite: **PASS, 86/86** (completed earlier in this run; no contract source changed in the final UI correction).
- Boards HUI Release suite: **PASS, 34/34** (completed earlier in this run; no HUI source changed in the final UI correction).
- CUI markup tests: **PASS, 4/4** (completed earlier in this run).
- `git diff --check`: **PASS**; Git printed LF-to-CRLF normalization notices for existing dirty files.

The Release commands used the task-local .NET 10.0.301 SDK, `--no-restore`, invocation-only CUI build-task paths, and `TreatWarningsAsErrors=false` because vendored Avalonia emits AVADEV1001. `UsedAvaloniaProducts=` was passed to bypass Avalonia’s telemetry task, which otherwise attempted to write its log under protected `%LOCALAPPDATA%`. No project configuration was changed for these workarounds.

### Visual QA

- Headless Maths captures rendered at 1280×720, 1440×900, and 1920×1080; Maths Dark, Law, and Computer Science rendered at 1440×900.
- Assertions verify the page surface fills the EditorScroll viewport, content inset stays at 40×32 px, and the page has no card border, padding, outside margin, or corner radius.
- Light and Dark captures were visually inspected. Boards resolves `CuiTheme.Glow` and publishes the shared accent gradient; the loaded Maths fixture reports `Loaded locally`.
- **Native interactive launch: UNVERIFIED.** The Computer app inventory was empty. Headless render evidence does not prove live pointer input, Draw interactions, or pointer-trail behavior.

## Dirty checkout and preservation

- The checkout was already dirty at the start of this run (30 tracked dirty paths and an untracked `docs/architecture/browse.md`). After adding this ledger, `git status --short` contains 47 status entries, including unrelated user work; all unrelated files remain untouched.
- The local `main` HEAD is still `108404713bdd6a1a90400e5357abd63d38e65bf7`; audited upstream `main` was `53935fb772c2c57cb105945464dddfb267dd2264`. Boards source restored for continuation remains working-tree content (including untracked app/contract files) and has not been committed or merged.
- The already-dirty `framework/CUI/Runtime/CuiControlLoader.cs` was preserved and received narrowly scoped compatibility changes; the added regression test is in `framework/CUI/Runtime.Tests/CuiToAvaloniaPipelineTests.cs`.
- No commit was created during this run.

## Remaining Phase 01 work

- **UNVERIFIED:** native Boards app launch and live editing in a Windows desktop window.
- **UNVALIDATED:** pointer trail/gap/flicker behavior and the specified draw-over-content flow.
- Continue the remaining Boards release gates for navigation/workspace switching, creation UX, icons, custom themes, and end-to-end persistence. Do not promote Phase 01 until its release gate is met with direct evidence.
- After Phase 01 is complete, continue in the programme’s prescribed order with Phase 02 — Home + shared platform/AI ecosystem.

## Progress checkpoint — 2026-09-23 05:10 UTC

- **Current phase:** Phase 01 — Boards remains IN PROGRESS. Edge-to-edge seamless surface, 40 px horizontal / 32 px vertical content inset, shared Glow palette, selected-page ink/viewport persistence, accessible New menu, separate board creation, and anchored insertion are implemented.
- **Boards Release evidence:** dotnet vstest on the current CakeOS.Apps.Boards.App.Tests.dll passed **45/45**. The run includes six visual captures at 1280×720, 1440×900, and 1920×1080, selected-page ink/save/reopen, board creation, and insertion-order persistence. I inspected the 1440×900 Maths capture. The worker’s immediately preceding dotnet test also passed **45/45**.
- **Boards validation still open:** CUA reports no native Windows app surfaces. Live pointer input, file-picker interaction, and pointer trail/gap/flicker behavior remain UNVERIFIED/UNVALIDATED. A separate root rebuild reached the app build but could not write the coverage mapping file under the protected OneDrive test output directory; the fresh VSTest run used the worker-built current Release assembly.
- **Home:** one package-refresh recovery slice is implemented in HomeDashboard.cs; the worker reports Release **7/7** plus a persistence-of-last-observed-rows check. Home as a full phase remains IN PROGRESS; native window interaction was not exercised.
- **Maps / Forms:** the worker identified the existing Data app and its IDataWorkbookRepository as the real donor. The worker is adapting Forms submissions and Maps saved places to Data workbooks while preserving OSM behavior. The latest donor edits are not yet validated.
- **Terminal:** Windows ConPTY implementation and lifecycle specs are in progress. Host capability is Windows 10.0.26200; compilation and focused tests are still pending.
- **Canvas:** implementation lane has started in parallel under the user’s app-level worker request.
- **Parallel limit:** the runtime allows at most four active agents including the coordinator. The available slots are assigned to Boards integration, Maps/Forms, Terminal, and Canvas/Home work as lanes complete. Twenty-eight simultaneous agents are unavailable here.
- **Roadmap accounting:** Image→Pictures, Audio→Wave, and Video→Motion remain single product mappings. The requested 28-area inventory remains open; no suite-wide completion is claimed.
- **Repository safety:** branch main, HEAD 108404713bdd6a1a90400e5357abd63d38e65bf7; existing dirty scope is preserved, and no commit, push, reset, stash, or branch change was made.
- **Exact next action:** finish the current Maps/Forms, Terminal, and Canvas source slices, collect fresh focused test results, then update this ledger and continue the next available app lane. Keep native UI and platform checks explicitly queued until a Windows app surface is available.

## Takeover checkpoint — 2026-09-23 (supersedes the 05:10 downstream queue)

- **Phase:** 01 Boards remains **IN PROGRESS**. Phase 02 Home/shared ecosystem and later app lanes contain substantial dirty implementation, but none is promoted to READY by this checkpoint. The 05:10 worker allocation and pending Maps/Forms/Terminal/Canvas work above are historical, not the current queue.
- **Repository:** canonical OneDrive checkout, `main` at `108404713bdd6a1a90400e5357abd63d38e65bf7`, 18 commits behind `origin/main`; at intake 109 tracked dirty paths (including the existing `apps/Home/ui/Home.cui` deletion), 54 untracked files, zero staged entries, 163 status paths. After the Core catalogue regression fix: **110 tracked + 54 untracked = 164**, zero staged. No merge, pull, reset, stash, commit or push. Preserve every dirty path.
- **Recovery snapshot:** before takeover edits, `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-dirty-20260923-takeover.zip` contains `git diff --binary HEAD` and all 54 untracked files; ZIP integrity passed; SHA-256 `0322f7c04037f5f920ab836f4f1bf4e2a59918fe6770526c5b3b1a0f64e48a5a`. This is an external recovery artifact, not a release package.
- **Toolchain:** Windows 10.0.26200, .NET SDK 10.0.401 (compatible with `global.json` roll-forward); invocation-local `TEMP`/`TMP=C:\Users\Jacob\AppData\Local\Temp\opencode`, `AVALONIA_TELEMETRY_OPTOUT=1`. `dotnet build framework/CUI/Compiler/CakeOS.Cui.Build.Tasks/CakeOS.Cui.Build.Tasks.csproj -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS**, 0 warnings/errors. The resulting *vendored CakeOS* build-task DLL was passed through `-p:CuiBuildTasksLocation=<repo>\framework\CUI\Compiler\CakeOS.Cui.Build.Tasks\bin\Release\netstandard2.0\CakeOS.Cui.Build.Tasks.dll`; no incompatible package DLL substituted or build property persisted.
- **Boards:** corrected selection reset on page/section navigation, including legacy commands and post-navigation reselect; formatting a new empty page creates an editable paragraph. `dotnet test '9to1 Workspace/Boards/app.tests/CakeOS.Apps.Boards.App.Tests.csproj' -c Release --no-restore -p:CuiBuildTasksLocation=<vendored DLL above> -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 46/46** after the final Boards edit. Its focused empty-page regression **PASS 1/1** before the final selection follow-up. Headless UI evidence does not establish live interaction.
- **Boards contracts/HUI recheck:** `dotnet test '9to1 Workspace/Boards/tests/CakeOS.Apps.Boards.Tests.csproj' -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 86/86**; `dotnet test '9to1 Workspace/Boards/hui-tests/CakeOS.Apps.Boards.Hui.Tests.csproj' -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 34/34**. Both rebuilt from the current dirty checkout.
- **9-1 App / Agents / Overlay:** launcher sections preserve pin/usage ordering; the test now matches section nodes rather than their `.Grid` descendants. Agents activity log privacy, size, invalid-event and recent-run ordering regressions were added. Overlay now arranges its shell before focusing the newly visible collapsed/expanded target; focus-style assertions check state targets instead of a transition's first animated sample. `dotnet test '9to1 Workspace/shared/tests/Haven.Desktop.Tests/Haven.Desktop.Tests.csproj' -c Release --no-restore --filter 'FullyQualifiedName~AppLauncherCoverageTests|FullyQualifiedName~AgentsHavenSceneTests|FullyQualifiedName~OverlayWorkspaceWindowTests' -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 15/15**; the same command without `--filter` **PASS 781/781**, zero test failures/skips after final edits.
- **Shared CUI:** `dotnet test framework/CUI/Runtime.Tests/CakeOS.Cui.Runtime.Tests.csproj -c Release -p:CuiBuildTasksLocation=<vendored DLL above> -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 52/52**. `git diff --check` **PASS** (existing working-tree LF→CRLF notices only).
- **Shared integration:** `dotnet test '9to1 Workspace/shared/tests/Haven.Infrastructure.Tests/Haven.Infrastructure.Tests.csproj' -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 374/374**. The first equivalent Core run found **1 failure / 545 passed**: the catalogue invariant omitted the authorised new Forms app. Updated `ModeIdentityTests` to expect `forms` last and assert its persisted stable ID `a0000000-0000-0000-0000-000000000026`; `dotnet test '9to1 Workspace/shared/tests/Haven.Core.Tests/Haven.Core.Tests.csproj' -c Release --no-restore -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 546/546**. No application IDs were changed.
- **Previous worker reports:** Automations, Data, Maps/Forms, Canvas, Home, Write, Planner, Mail, Browse, Pictures, Wave, Motion, Spaces, Mesh, Voice, Generative UI, Background Learning, Translate, Play, Terminal, and other app-specific counts in the migration handoff remain **UNVERIFIED at suite-wide release level** until their current source is built/tested and routes exercised. Files is explicitly retained in the primary programme; Image=Pictures, Audio=Wave, Video=Motion. Present lifecycle changes are subject to the previously reported approval decision.
- **Worker model constraint:** operator is GPT-6 Sol; this session's child-agent invocation has no model configuration or model-inspection field, so GPT-6 Luna cannot be explicitly set/verified here. No child agents were launched. This does not block direct operator implementation or verification.

### WINDOWS / LOCAL VALIDATION QUEUE (current working-tree diff at the HEAD above)

1. **Boards Phase 01, Windows interactive:** launch the current `9to1 Workspace/Boards/app` Release build with representative Maths/Law/CS `.9to1board` documents; exercise create/open/save/reopen, empty-page formatting, keyboard navigation, New menu, inline insertion, pointer drawing, stylus pressure, scrolling/resizing ink alignment, theme change and file picker. **UNVERIFIED**: no Computer/native app surface is exposed in this session; 46/46 headless tests cannot prove physical input or pointer trail/flicker behavior.
2. **Home/shared shell, Windows interactive:** open the actual launcher, Agents and Overlay routes; check pin ordering across categories, log privacy, expanded/collapsed focus, focus ring after transition, keyboard access and high-contrast/four appearances. **UNVERIFIED**: headless 781/781 covers only exercised scene paths; no live GUI surface exposed.
3. **Terminal, Windows Visual Studio replay:** reproduce the original `PtyByteCapture.WaitForTextAsync` working-directory startup and JIT-dialog case with the current ConPTY flag change, then perform resize/exit/disposal in a physical shell. **UNVERIFIED** for the original dialog, despite earlier worker-reported focused suite success.
4. **Data/Forms/Maps and ecosystem:** run Calc-backed HUI with `soffice` and Python `uno` installed, exercise donor workbook persistence and lossless reopen; run affected Debug/Release suites and packaging/VM/device gates. **BLOCKED** for Calc-backed runtime by absent `soffice`/Python `uno`; other native/device checks **UNVERIFIED**.

**Next engineering action:** continue framework-first dependency stabilization and Boards Phase 01 release gate (navigation, icons/custom themes, end-to-end persistence), then expand independent app surface work in primary phase order while keeping all prior dirty paths intact. Run the full affected solution gates before any READY promotion.

## Resume checkpoint — Boards insertion safety (2026-09-23)

- **Phase 01 remains IN PROGRESS.** `InsertKindAsync("freeform")` now creates only the page-level freeform object, without an unrelated paragraph. The attachment picker runs before inserting a paragraph; an absent/cancelled picker leaves the page unchanged, and a page switch while the picker is open aborts insertion rather than attaching to the new page. A successful attachment uses the same selected file without opening a second picker.
- **Persistence evidence:** new tests save, close and reopen a real `.9to1board` for both the freeform object and the attachment, then resolve and verify the attachment payload bytes. Focused `BoardsCreationAndInsertionTests` **PASS 9/9** before the final payload assertion; the subsequent full suite includes that assertion. Focused Release command: `dotnet test '9to1 Workspace/Boards/app.tests/CakeOS.Apps.Boards.App.Tests.csproj' -c Release --no-restore --filter 'FullyQualifiedName~BoardsCreationAndInsertionTests' -p:CuiBuildTasksLocation=<vendored DLL above> -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` with the task-local environment described above.
- **Affected full gate:** same Release command without `--filter` **PASS 50/50**, 0 failures/skips. Contracts/HUI/CUI and Haven shared suites were verified at the takeover checkpoint and are not claimed rerun after this app-only change. Native file-picker and pointer/stylus interaction are still **UNVERIFIED**; the existing Windows/local queue remains active.
- **Next action:** finish Boards navigation/workspace switching and custom-theme/icon checks with focused acceptance evidence, then physical Windows UI verification when a Computer/native app surface is available. Do not promote Boards to READY on these tests alone.

## Authoritative surface inventory — 29 distinct entries

- The supplied specification contains **32 roadmap labels**, but the user clarified that only **19 Workspace + 10 AI = 29** are distinct intended surface entries. `docs/9TO1_SURFACE_MAP.md` is the corrected surface-wave map. **9-1 App** is another name for **Home**; **Product Launch** refers to the actual project launch later; **Custom Harnesses** is a deferred **Studio feature** for building custom AI harnesses. None is a separate surface-worker lane. Image/Pictures, Audio/Wave and Video/Motion each denote one product. Files remains a required additional primary-programme phase outside this roadmap transcription.
- **Surface wave: NOT STARTED.** Shared framework convergence and safe file ownership are not established as complete. Moreover, the available child-agent tool cannot explicitly set or inspect a child's model; GPT-6 Luna compliance cannot be verified, so no child-agent wave has been launched. Continue direct Sol implementation/integration and admit independent surface lanes only after the framework gate and Luna configuration are genuinely available.
- **Git state after inventory:** `main` at `108404713bdd6a1a90400e5357abd63d38e65bf7`, 18 behind remote; 110 tracked dirty paths, 55 untracked files (the additional path is this surface map), zero staged entries. `git diff --check` passes. The pre-takeover snapshot remains the recovery baseline for the original 163 entries; this map and subsequent edits are live checkout work and are not included in that earlier archive.

## Framework-first continuation — CUI / Android / Home (2026-09-23)

- **Release test gates on current checkout:** `dotnet test framework/CUI/AI.Tests/NineToOne.Cui.AI.Tests.csproj -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 4/4**; equivalent command for `framework/CUI/tests/NineToOne.Cui.Markup.Tests.csproj` **PASS 4/4**, `9-1 OS (Android)/tests/Haven.Android.Cui.Tests/Haven.Android.Cui.Tests.csproj` **PASS 7/7** (portable .NET test target, **not** an Android device run), `9to1 Workspace/Home/Tests/HavenOS.Home.Tests.csproj` **PASS 8/8**, and `9to1 Workspace/Spaces/Tests/HavenOS.Spaces.Tests.csproj` **PASS 12/12**. Each restored/built its current project graph on Windows with task-local `TEMP`/`TMP` and Avalonia telemetry opt-out as above.
- **Native Home host compile:** `dotnet build apps/Home/src/AvaloniaHome/AvaloniaHome.csproj -c Release -p:CuiBuildTasksLocation=<vendored DLL above> -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS**, 0 warnings/errors. Equivalent **Debug** build **PASS** with **one pre-existing vendored Avalonia AVADEV1001 warning** in `Avalonia.Base/Utilities/Ref.cs`, 0 errors. After replacing the misleading “window rendered” startup log with “CUI loaded into window”, Release `--no-restore` build again **PASS**, 0 warnings/errors. The old missing-public-key/MSB4006 Home-build blocker in `docs/MASTER-MIGRATION-STATUS.md` has been corrected there; no vendor files or global NuGet configuration changed.
- **Core orchestration script:** direct `& './eng/9to1.ps1' -Action test -Component core -Configuration Release` was **BLOCKED by this host's PowerShell script execution policy**, before the script ran. Its three .NET test project gates (CUI markup, Home, Spaces) were executed directly above; do not report the script itself as passing or alter machine policy to force it.
- **Framework pass: INCOMPLETE.** Compile/unit coverage does not establish native Home route rendering, physical interaction, Android device semantics or cross-surface theme/lifecycle acceptance. No Computer/native app surface is exposed here, and no explicitly configurable/verifiable GPT-6 Luna child-agent interface is available. The corrected 29-entry surface map stays queued; no worker wave has started.
- **Next action:** validate Home's actual CUI route in a native GUI when available and continue remaining shared framework runtime/theme/Android parity gates; then admit independent surface work with isolated ownership only when Luna worker configuration is verifiable. Preserve existing dirty work.

## Framework verification / Android repair (2026-09-23)

- **Execution policy:** on the user's explicit instruction, ran `powershell -NoProfile -ExecutionPolicy Bypass -File 'eng/9to1.ps1' -Action test -Component core -Configuration Release` with task-local `TEMP`/`TMP` and `AVALONIA_TELEMETRY_OPTOUT=1`. **PASS**: markup 4/4, Home 8/8, Spaces 12/12. This is an invocation-only policy override; no machine/user policy change.
- **Repository verify:** same invocation with `-Action verify -Component core -Configuration Release` **PASS** for the same 24 tests plus package preload metadata, release graph, immutable lock, staging mapping, and copy-only policy. Package *bytes* **UNVERIFIED** pending staged `.deb` artifacts (`-RequireArtifacts`); script reports **8 tracked CUI, 68 AXAML, 30 HUI** files and correctly warns that the CUI-only migration is unfinished. Did not pass `-RequireCuiOnly` or weaken the gate.
- **Shared solution tests:** same invocation with `-Action test -Component shared -Configuration Release` **PASS**: Haven.UI 162/162, Core 546/546, Infrastructure 374/374, Desktop 781/781 (**1,863 tests**, zero failures/skips). Before repair, restore printed a missing `Haven.Android.Hui` project warning even though the test command exited successfully.
- **Android build defect found and repaired:** direct `dotnet build '9-1 OS (Android)/src/Haven.Android/Haven.Android.csproj' -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` initially **FAIL**: missing `../Haven.Android.Hui/Haven.Android.Hui.csproj` (MSB9008) and five missing adapter-operation type errors in `AndroidHuiCanvasRenderer.cs`. The referenced portable adapter and its bounded structural/unsupported-command tests had been deleted in commit `9b6e2a3`; recovered the original four files from its parent into their original paths, with the missing explicit `using Xunit;` needed by the restored test project. No new rendering architecture or fake unsupported-command implementation was introduced.
- **Post-repair validation:** `dotnet test '9-1 OS (Android)/tests/Haven.Android.Hui.Tests/Haven.Android.Hui.Tests.csproj' -c Release -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS 5/5**; `dotnet build '9-1 OS (Android)/src/Haven.Android/Haven.Android.csproj' -c Release --no-restore -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false` **PASS**, 0 warnings/errors; `powershell -NoProfile -ExecutionPolicy Bypass -File 'eng/9to1.ps1' -Action build -Component shared -Configuration Release` **PASS**, 0 warnings/errors. Android device launch/input/launcher lifecycle remain **UNVERIFIED**; a compile is not a device pass.
- **Working tree:** `main` at `108404713bdd6a1a90400e5357abd63d38e65bf7`; 110 tracked dirty paths, 59 untracked files (four restored adapter/project/test paths added), zero staged. `git diff --check` **PASS**. The original recovery snapshot predates this repair. No commit or push.
- **Framework status: INCOMPLETE.** The broken Android dependency graph is repaired, but the CUI-only migration, native Home interaction, Android device validation, package-byte inspection and other full release gates remain open. Surface-worker wave remains pending both framework convergence and explicit GPT-6 Luna configuration/verification.

## Framework-to-Home route validation (2026-09-23)

- **Defect:** loading the actual canonical `Home.cui` into the native host under Avalonia Headless showed that the visual route was measured, but every authored `id` was missing from native control names/automation IDs. `CuiRichParser` already stores this as `CuiComponent.Name`; `CuiControlLoader` had only applied entries from `component.Properties`. The shared loader now projects authored component IDs into Avalonia `Control.Name` and the default `AutomationProperties.AutomationId`, allowing an explicitly authored `automation-id` to override the latter. Added a CUI Runtime regression covering both IDs and override precedence; updated `docs/architecture/cui.md`.
- **Native-host headless evidence:** new `apps/Home/tests/AvaloniaHome.Tests` invokes the production `HomeApp.BuildWindow()` under the vendored headless Avalonia backend, finds the canonical `UI/Home.cui`, shows and arranges a 1200×800 window, asserts Home navigation/hero/app-card controls exist, and checks observed catalog/runtime unavailable bindings. It invokes the actual Settings navigation button and verifies the host reports “not connected” instead of feigning navigation. This tests the real Home host's CUI loading/action path, **not** a physical Windows compositor, pointer, packaging or full route navigation.
- **Commands and results:** with task-local `TEMP`/`TMP`, `AVALONIA_TELEMETRY_OPTOUT=1` and `-p:CuiBuildTasksLocation=<locally built vendored CakeOS DLL> -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false`: `dotnet test apps/Home/tests/AvaloniaHome.Tests/AvaloniaHome.Tests.csproj -c Release --no-restore` **PASS 1/1**; same command `-c Debug --no-restore` **PASS 1/1** after the Settings-action assertion; `dotnet test framework/CUI/Runtime.Tests/CakeOS.Cui.Runtime.Tests.csproj -c Release --no-restore` **PASS 53/53**; `dotnet test '9to1 Workspace/Boards/app.tests/CakeOS.Apps.Boards.App.Tests.csproj' -c Release --no-restore` **PASS 50/50** after the shared loader change. `git diff --check` **PASS**.
- **Status:** framework and Phase 02 Home still **INCOMPLETE**. Headless rendering demonstrates a real arranged route but no screenshot was captured/inspected and no live GUI/Android interaction, global theme persistence or package acceptance is claimed. The corrected 29-surface wave remains pending the same Luna configuration and remaining framework gates.
- **Checkout:** `main` at `108404713bdd6a1a90400e5357abd63d38e65bf7`; 110 tracked dirty paths, 61 untracked files, zero staged entries. The pre-takeover recovery snapshot predates this pass; no reset/clean/commit/push.

## Worker deployment preflight (2026-09-23)

- **Readiness: GPT-6 Luna availability CONFIRMED by the user's running OpenCode picker; child verification still PENDING.** The picker shows OpenAI GPT-6 Luna beside GPT-6 Sol and GPT-6 Astra. The earlier CLI `models openai --refresh` output is stale/incomplete for this desktop runtime and is not evidence of unavailability. The current `task` tool still exposes no effective-model selector or inspection result. Determine the exact provider/model ID from running metadata, configure one isolated child explicitly, and verify its session/model metadata before assigning implementation work; do not substitute GPT-5.6 Luna or create a GPT-6 Sol child.
- **Recoverability:** refreshed external preflight snapshot `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-dirty-20260923-worker-preflight.zip`, SHA-256 `b54852492af1c1c10bb44c11f602c7c5598f6b25475a7f949fe9dfb0396c6107`, ZIP integrity checked; contains current binary-capable tracked patch and 61 untracked files before adding `docs/9TO1_WORKER_DEPLOYMENT.md`. `git worktree list --porcelain` shows only the canonical dirty checkout. A new clean `HEAD` worktree would lack the substantial current untracked app source, so it cannot be used as an identical worker baseline.
- `docs/9TO1_WORKER_DEPLOYMENT.md` records admission checks, baseline partitioning, framework evidence and one grounded Boards pilot deliverable with owned paths/acceptance. Canvas/Home candidate work is not fabricated from already implemented recovery tests. Before adding lanes, identify real outstanding independent changes. The three void labels remain excluded per `docs/9TO1_SURFACE_MAP.md`.
- **Distance to deployment:** model availability itself is resolved. An isolated pilot still requires exact model-ID/effective-child verification and a proven dirty-state worktree baseline. The full surface wave additionally depends on contract freeze/ownership and remaining framework/platform gates; no calendar ETA or phase completion is asserted.
- **Post-preflight status:** 110 tracked dirty paths, 62 untracked paths (including this deployment document), zero staged; `git diff --check` **PASS**. The preflight ZIP is a recoverable snapshot of the preceding 61 untracked paths, not of this newly added document.

## Luna deployment correction and isolated probe (2026-09-23)

- **Resolved availability:** the user's running desktop model picker shows OpenAI GPT-6 Luna alongside GPT-6 Sol; the running desktop log independently records `providerID=openai modelID=gpt-6-luna`, establishing **`openai/gpt-6-luna`**. The standalone `opencode.cmd` 1.17.15 catalog is stale: its fresh process rejects GPT-6 model IDs, so its failure does **not** overturn desktop availability.
- **Explicit configuration:** added canonical `.opencode/opencode.json` pinning built-in `general` and `explore` subagents to `openai/gpt-6-luna`, plus `.opencode/agent/luna-model-probe.md` with `mode: subagent`, the same model and denied edit/bash/task permissions. The CLI recognizes all three agent definitions. OpenCode config is loaded at startup; the current running desktop session was opened from `C:\Github\9to1` and cannot be assumed to have loaded this canonical project config. **No effective child session/model metadata has yet been verified.** No GPT-6 Sol child was launched.
- **Preservation/isolation:** `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-dirty-20260923-luna-config.zip` contains the tracked binary patch and 64 untracked files, integrity checked, SHA-256 `201bcaf4ca2f7f57ee45b4e494aaad869f92cf4664ae4622163f43b467e1ec10`. Added a detached, clean HEAD model-probe worktree under `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-luna-probe` with only its explicit read-only Luna agent config. It is not a worker implementation baseline and has not received the dirty Boards source.
- **Next exact gate:** restart the desktop OpenCode session with the canonical OneDrive checkout open; invoke one read-only isolated child, then inspect its session `parent_id`, agent name and `{providerID:"openai",id:"gpt-6-luna"}` before granting any implementation task. After that, prove a safe owned dirty-source worktree baseline and freeze shared contracts before expanding workers. The 29-surface wave remains **NOT STARTED**; remaining issues are verification/isolation/framework/ownership, not model availability.
- **Current canonical status:** 110 tracked dirty, 64 untracked, zero staged; no commit/push/reset/clean. Recovery snapshot predates this final ledger update only.

**Restart follow-up:** the user restarted OpenCode, but the resumed conversation still reports project directory `C:/Github/9to1`; separate canonical OneDrive GPT-6 Sol/Luna sessions in the desktop database have `parent_id=null` and do not prove a child. The canonical `.opencode` config cannot be assumed effective for an agent invoked from this stale-project conversation. Read-only child verification remains **PENDING** until a Sol operator conversation is active in the canonical OneDrive project; do not risk an inherited Sol child.

## Authority correction and two-checkout reconciliation (2026-09-23)

- **Current authority:** the user explicitly identified `C:\Github\9to1` as the development checkout. This Sol operator conversation already runs there (`session.directory` and shell cwd); the earlier assertions that OneDrive was canonical are historical errors. OneDrive remains a preserved preparation duplicate; neither checkout was reset/cleaned/deleted. Github originally sat clean at `53935fb772c2c57cb105945464dddfb267dd2264`, 18 commits ahead of the OneDrive HEAD `108404713bdd6a1a90400e5357abd63d38e65bf7`.
- **Pre-reconciliation recoverability:** Github clean-head ZIP at `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-authority-github-before-reconcile.zip` (SHA-256 `e7a5da47642c36b0482b33edf4287bb23a5c5e3ee0d69b7f787e5c9f54aa29ac`); OneDrive dirty tracked binary patch + 64 untracked files at `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-onedrive-before-reconcile.zip` (SHA-256 `84e5e6161f7e457965d889a239a762ee0f697404287b2b3080728a2ad1fc5680`). Both ZIP integrity checks passed. Original Git history also retains the Github pre-reconcile source.
- **Source accounting:** all 174 OneDrive dirty status paths were classified before edits: 98 tracked paths not changed upstream, 11 tracked paths changed on both sides, 16 OneDrive-untracked files identical to Github HEAD, 15 differing Boards app files already present upstream, 33 genuinely absent paths, and one deliberate Home demo-file deletion. Applied the clean tracked patch; merged compatible changes; manually preserved Github-only CUI loader support while integrating component IDs, attached Grid coordinates, authored text/actions and Glow resource fallbacks; copied 33 absent paths and the 15 reviewed Boards variants with hash checks. The legacy `apps/Home/ui/Home.cui` was a separate demo and is recoverable from Git history; canonical `9to1 Workspace/Home/UI/Home.cui` remains. **173 of 174 OneDrive dirty paths now match the Github working tree after newline normalization; only the intentionally combined CUI loader differs.** The 18 newer Github commits and their unique source remain in history.
- **Ignored build inputs:** OneDrive had 25 ignored vendored Avalonia `build/` inputs needed to compile; Github lacked them. Copied 24 byte-identical inputs and provided the equivalent public-key props (newline-only difference). Four generated Win32 interop files and the generated `.opencode/.gitignore` were independently found byte-identical. Other ignored `bin`/`obj` files are generated output; neither checkout has been discarded.
- **Merged-checkout validation on `C:\Github\9to1`:** CUI Runtime Release **53/53**, Boards app Release **50/50**, native Home headless Release **1/1**, shared solution Release **1,863/1,863**, and Android Release build **PASS**, 0 warnings/errors, with task-local `TEMP`/`TMP`, `AVALONIA_TELEMETRY_OPTOUT=1` and invocation-only vendored CUI build-task location where needed. These validate tested routes/builds, not physical Windows or Android interaction.
- **Concurrent commit detected:** `main` moved to `d949fcf23f10b75694d4b6614108c0d92eada9c4` (`Summary (required)`) during this pass, outside this session's commit tools. It contains the source reconciliation; **this operator did not create, amend or push it**. Review its provenance separately if needed. Do not rewrite or discard it. Immediately after it, only corrections to this ledger and `docs/9TO1_WORKER_DEPLOYMENT.md` remained uncommitted.
- **Operator/worker config:** Github `.opencode/opencode.json` now pins `build` to `openai/gpt-6-sol` and `general`/`explore` to `openai/gpt-6-luna`; a read-only Luna probe subagent exists. CLI agent discovery succeeds, and the current Github operator session metadata reports Sol. Project config reload and a real child session's `parent_id`/Luna model verification remain **PENDING**; no Sol child or worker wave was launched.

**Final copy audit:** the OneDrive duplicate still has 110 tracked dirty paths and 64 untracked files at `1084047`; it has not been deleted. Every one of its 174 status paths is accounted for in Github. None of its present files is missing in Github; `apps/Home/ui/Home.cui` is deleted in both working trees and retained in Git history. After newline normalization, the only five differences among those paths are the deliberately combined `framework/CUI/Runtime/CuiControlLoader.cs`, the Sol-pinned Github `.opencode/opencode.json`, and three ledgers corrected to name Github as authority (`docs/MASTER-MIGRATION-STATUS.md`, `docs/9TO1_COMPLETION_STATE.md`, `docs/9TO1_WORKER_DEPLOYMENT.md`). The 25 ignored vendor-build inputs were checked; four generated Win32 interop files and `.opencode/.gitignore` match. Other ignored `bin`/`obj` outputs stay in their original copy.

**Post-reconcile recovery:** `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-authority-github-reconciled.zip`, SHA-256 `dbd38815b77c086bdbb7094f9c6d8ad1a358fbf3f36bf5f9238a3c7187d2b028`, ZIP integrity checked. It records current Github HEAD, its uncommitted ledger patch and all 25 ignored vendor-build inputs. This is in addition to the before-reconcile snapshots and Git history; neither directory was discarded. `git diff --check` passes; only the three ledger corrections are unstaged at `d949fcf`.

**Mandatory child-model gate FAILED:** the one read-only `general` probe returned the correct Github/Boards facts but desktop session metadata (`ses_f31223c80ffevgTaU0q7j2BwAg`, parent `ses_f32966188ffeUDrcXO37edgDeS`) records **GPT-6 Sol**, not Luna. The runtime log corroborates it. This is an explicit **STOP** before framework delegation or worker deployment; no implementation children were launched. The project config pins `general` to GPT-6 Luna and the standalone CLI resolves it correctly, but the running desktop session predates that config-time change. Require a full desktop restart from `C:\Github\9to1` and a fresh successful read-only child with `providerID=openai`, `id=gpt-6-luna` before further delegation.

## Latest framework checkpoint — 2026-09-23

- **Authority / model gate:** `C:\Github\9to1`, `main` at `0ff7867cf22fee6927b2608e26a616277c2e590b`. A subsequent read-only `general` child (`ses_f311d037effecHJ47KoVo4KRN3`) is independently recorded by desktop session metadata and runtime log as `openai/gpt-6-luna`; it supersedes the historical failed Sol probe above. No surface implementation worker has started.
- **Shared CUI theme repair:** `ApplyGlobalTheme(theme)` applies the requested palette and updates the active catalog theme. Theme-name resolution uses its explicit global-default argument; the CUI loader snapshots the active theme at load time, with explicit nested scopes retained. The contract/owner boundary is recorded in `docs/architecture/cui.md` and the focused regression covers a loader constructed before a theme change. This does not implement persisted, cross-process theme preference.
- **Windows Release evidence after repair:** CUI Runtime **56/56**, Boards app **50/50**, native Home headless **1/1**. Repository core verify **PASS**: markup **4/4**, Home **8/8**, Spaces **12/12** and package-preload metadata/release graph. Verify reports **8 CUI, 68 AXAML, 30 HUI** tracked files and explicitly did **not** inspect package bytes. The task-local .NET environment used vendored CUI build tasks and Avalonia telemetry opt-out for the relevant native suites. `git diff --check` **PASS**. HEAD was preserved and edits remain unstaged.
- **Framework convergence remains INCOMPLETE:** native Home/Boards interaction and DPI/accessibility, Android device/launcher behavior, global theme persistence and cross-surface application, live DevTools inspection, legacy AXAML/HUI migration boundary acceptance, Linux rendering and staged package-byte acceptance still require implementation or direct validation. Build/headless results do not promote framework or Phase 01 Boards to READY.
- **Local platform availability:** the Windows host has the .NET 10 SDK and Android workload, but no `adb` executable was found on `PATH`; `wsl --list --quiet` returned no installed Linux distribution. This environment cannot supply Linux render or Android-device acceptance evidence through those tools. No physical GUI interaction was observed in this session.

**Further shared-theme continuation:** Confirmed `Haven.Desktop/UserPreferencesService` already owns the persisted theme choice. Added a read-only CUI reader for its `havenUiThemeName` and wired native Home window builds and Boards startup to consume it, with Boards light/dark appearance remaining independent. Headless Home exercises an updated preference between builds; CUI Runtime **57/57**, Home headless **1/1**, Boards app **51/51** Release tests pass. This is persisted preference consumption at launch/rebuild, not live propagation to all processes/surfaces; framework and Phase 01 remain **INCOMPLETE**.

**Loader/DevTools continuation:** Reset CUI document resources and tracked bindings when a loader is reused; the Runtime Release suite now passes **58/58**. Repaired pre-existing DevTools compile errors and connected a read-only Avalonia tree/layout/accessibility snapshot to the native Home window; its headless route remains **PASS 1/1** with the actual Settings navigation element inspected. Full live DevTools tracing and physical platform interaction remain open; **framework convergence is still INCOMPLETE**.

**Further affected-host checks:** Boards app Release **51/51** after the loader change; Android Release app build **PASS**, 0 warnings/errors. This is Android compile coverage only; no device, Linux rendering or package-byte acceptance was exercised. No staged `.deb` files are present under `9to1 OS`.

## Latest DevTools/framework handoff — 2026-09-23

- **Current checkout:** canonical `C:\Github\9to1`, `main` at `0ff7867cf22fee6927b2608e26a616277c2e590b`; no reset, clean, stage, stash or commit. The single read-only GPT-6 Luna child gate remains verified; no surface-worker wave started. Framework remains **INCOMPLETE / NOT READY**.
- **Host-available DevTools contract:** the loader preserves authored CUI component ID/type/source span and last binding resolution while applying controls. The read-only Home inspector now observes actual control tree/layout, CUI source/attribute provenance, binding path/mode/value/error/fallback, action command/wiring/explicit route availability, native effective style and semantic resources, automation-peer role/name/states, and rendering geometry/visibility/opacity/scale. A registered handler is not treated as route availability. No diagnostic capture invokes an action or retries a binding getter. Home's actual route is covered under Avalonia Headless, including a faulted/missing binding on its canonical document and a theme-resource change on rebuild. Physical platform behavior and frame timings are not claimed.
- **Verification:** exact PowerShell invocations and task-local environment are in `docs/9TO1_WORKER_DEPLOYMENT.md` (latest checkpoint). CUI Runtime Release **58/58**; native Home headless Release **1/1**; Boards app Release **51/51**; Android app Release build **PASS, 0 warnings/errors**; `git diff --check` **PASS**. Remaining external evidence: physical Windows rendering/input/accessibility/DPI/theme interaction, Android device with adb, Linux native rendering, and staged `.deb` package-byte/install/launch checks. None is inferred from builds/headless tests.

## Surface-worker integration checkpoint — 2026-09-24

- Canonical `main` is at `f54fb57257acbc06084d968673dac51d450367e1`; eight bounded app-owned lanes (Boards, Canvas, Spaces, Terminal, Browse, Data, Pictures and Home Dashboard) have been integrated into its uncommitted working tree. Focused canonical Release evidence: Boards **52/52** (invocation-only `RunAnalyzers=false` workaround for vendored `AVADEV1001`), Canvas **5/5**, Spaces **13/13**, Browse **12/12**, Pictures **14/14**, Home Dashboard **10/10**, Terminal specs **PASS**, and Data cell-contract check **PASS**. Data's full runtime check remains blocked by missing LibreOffice `soffice`.
- The four platform-acceptance gates remain **OPEN**: physical Windows interaction/rendering/accessibility/DPI/theme, Android device/launcher, Linux native rendering, and `.deb` package-byte/install/launch. This is bounded app regression evidence, **not release readiness**.
