# Surface-worker deployment preflight

**State: GPT-6 Luna child model verified; model gate cleared (see latest checkpoint below).** The operator is GPT-6 Sol. The user confirms that the **running OpenCode desktop model picker** exposes OpenAI GPT-6 Astra, GPT-6 Luna and GPT-6 Sol together. The running desktop's `opencode.log` records `providerID=openai modelID=gpt-6-luna` and `llm.provider=openai llm.model=gpt-6-luna`; the exact identifier is **`openai/gpt-6-luna`**. The project pins this in `.opencode/opencode.json` for built-in `general` and `explore` subagents, plus a read-only `.opencode/agent/luna-model-probe.md`. The current `task` tool has no per-call model/session-inspection field, so inspect the resulting child session metadata before implementation. Never let a child inherit GPT-6 Sol or substitute GPT-5.6 Luna.

**CLI-versus-desktop boundary:** the separately installed `opencode.cmd` 1.17.15 recognizes the project probe agent but its stale model catalog rejects *both* GPT-6 Sol and GPT-6 Luna with `ProviderModelNotFoundError`; a CLI probe produced **no verified child**. This does not negate the running desktop's GPT-6 Luna availability. OpenCode loads agent/config files only at startup; restart the desktop Sol session on the **canonical Github checkout** after these project config changes before using the pinned `general`/`explore` agents.

**Post-correction observation:** the active operator conversation runs in `C:/Github/9to1`; its desktop metadata records `agent=build`, `providerID=openai`, `id=gpt-6-sol`. This canonical project's config now explicitly pins `build` to Sol and `general`/`explore` and the read-only probe to Luna. Config reload is still required for child-model overrides. A Luna child is **not yet verified**: require a new child row with a Sol parent session ID, `agent=luna-model-probe` (or another explicitly pinned Luna subagent), and `model={"providerID":"openai","id":"gpt-6-luna"}`.

**First post-restart child probe: FAIL / STOP.** One harmless read-only `general` child (`ses_f31223c80ffevgTaU0q7j2BwAg`) read the completion ledger and correctly returned the Github path and Boards phase. Its actual desktop `session.model` was `{"providerID":"openai","id":"gpt-6-sol","variant":"medium"}`, with `parent_id=ses_f32966188ffeUDrcXO37edgDeS`, `agent=general`, `directory=C:/Github/9to1`; the runtime log independently confirms `modelID=gpt-6-sol agent=general mode=subagent`. **This violates the mandatory Luna-only child rule.** No implementation worker or second probe was launched. The standalone CLI's resolved `general` config reports `openai/gpt-6-luna`, indicating that the project file is valid, but the desktop session has not loaded the newer override. Its current run started before the `d949fcf` reconciliation committed `.opencode/opencode.json`. Fully restart the **desktop OpenCode process** on `C:\Github\9to1` with a new Sol session after this config is present, then check the effective child model again before any implementation. Do not treat the probe's correct answer as model verification.

## Checkout and isolation gate

- Canonical checkout: **`C:\Github\9to1`**, `main` at `d949fcf23f10b75694d4b6614108c0d92eada9c4` after an external concurrent commit of the reconciled source. The OneDrive tree at `108404713bdd6a1a90400e5357abd63d38e65bf7` is a preserved duplicate; its old counts and snapshots below are historical, not the new authority. The Github checkout was clean at `53935fb` before reconciliation. No reset/clean/stash or deletion of either copy.
- Recovery artifact for **this** post-integration state: `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-dirty-20260923-worker-preflight.zip`, SHA-256 `b54852492af1c1c10bb44c11f602c7c5598f6b25475a7f949fe9dfb0396c6107`. Contains `git diff --binary HEAD` and all 61 untracked source/doc files; ZIP integrity checked. It is an external backup, not a branch or deployable worker baseline.
- Refreshed artifact after project Luna configuration: `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-dirty-20260923-luna-config.zip`, SHA-256 `201bcaf4ca2f7f57ee45b4e494aaad869f92cf4664ae4622163f43b467e1ec10`; ZIP integrity checked, binary-capable tracked patch plus **64 untracked files**. It is the newer recovery point; no commit/stash/pull/reset.
- An earlier **read-only model probe worktree** at `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-luna-probe` belongs to the preserved **OneDrive duplicate**, detached at `1084047`. Its config pins Luna, but it is **not an authoritative implementation worktree**. Start new worker worktrees from the reconciled Github checkout, with owned dirty source transferred and verified per lane.
- A clean worktree at the current Github `d949fcf` HEAD **has** the reconciled Boards, Home, Forms and Android source; only the three post-reconcile ledger corrections remain unstaged in the canonical checkout. Worker worktrees must still use explicit owned paths, the locally required ignored vendor build inputs and a focused passing acceptance command. Do not give multiple workers unrestricted write access to one checkout.

**Authority-reconciliation update:** a concurrent commit `d949fcf` now contains the reconciled implementation and `.opencode` Sol/Luna pins in the Github repository. The pre-reconcile OneDrive snapshots above remain recovery evidence, not the active baseline. The authoritative post-reconcile ZIP at `C:\Users\Jacob\AppData\Local\Temp\opencode\9to1-authority-github-reconciled.zip` (SHA-256 `dbd38815b77c086bdbb7094f9c6d8ad1a358fbf3f36bf5f9238a3c7187d2b028`) includes Github HEAD, remaining ledger edits and 25 required ignored vendor build inputs. `git worktree list --porcelain` for Github shows only Github; the older probe worktree remains attached to the preserved OneDrive duplicate. A Github worktree at `d949fcf` now **does include** the integrated Boards/Home/Forms/Android source; first worker lane still requires explicit ownership, local vendor build support and effective GPT-6 Luna session verification. No child has been launched from this current Sol session since the new project config was written (config requires restart).

## Framework admission gate

- Current source evidence: native Home Release build 0 warnings/errors and headless route Debug/Release 1/1, CUI Runtime Release 53/53, Boards app Release 50/50, shared solution Release tests 1,863/1,863, Android Release build 0 warnings/errors; see `docs/9TO1_COMPLETION_STATE.md` for exact commands and scope.
- These prove bounded compile/tests, **not** complete framework convergence. Remaining: one stable shared-CUI owner, legacy AXAML/HUI migration boundaries (verify reports 68 tracked AXAML and 30 HUI), native Home/Boards route interaction, Android device behaviour, global theme persistence and packaging acceptance. A bounded pilot may be considered only once shared contracts used by its lanes are frozen and documented; do not label the entire framework or product suite READY because a build passes.
- The worker topology uses **29 distinct entries** in `docs/9TO1_SURFACE_MAP.md`, plus separately required Files. Home includes the 9-1 App alias. Product Launch is a later milestone; Custom Harnesses is a deferred Studio feature. Neither gets a lane.

## First candidate implementation lane after gates pass

Each lane requires a GPT-6 Luna model check, its own isolated checkout, explicit file ownership, an actual implementation deliverable, focused verification, and a return of changed paths, commands/results and integration considerations. These are candidates, **not active assignments**.

| Lane | Owned area and concrete deliverable | Avoid / dependency | Acceptance |
| --- | --- | --- | --- |
| Boards | `9to1 Workspace/Boards/app/NavBuilder.cs` and dedicated `app.tests` navigation tests: eliminate cross-board navigation state shared between independent sessions, preserving section collapse/rename behaviour for each board. | Avoid `framework/CUI`, shared Haven projects, ledger, and contract schema; consume frozen CUI loader. | Release Boards app suite, navigation regression, `git diff --check`; native pointer/UI gate remains separate. |

No second lane is yet specified to implementation quality. Canvas persistence and Home dashboard recovery already have tests in the dirty tree; assigning their existing work as if it were a new deliverable would be busywork. Before adding lanes, identify a concrete remaining defect or acceptance gap in the owned app code, then state an implementable change and focused tests. Once a pilot integrates, refill with additional non-overlapping surface work, not with idle auditors. Home-owned AI features get separate lanes only after their shared Home files are explicitly partitioned. No worker edits the completion ledger; operator integrates and records evidence.

**Launch sequence:** restart OpenCode with the canonical checkout after the config-time changes; start the read-only `luna-model-probe` through a Sol operator in the isolated worktree; inspect the new child session's `parent_id`, `agent` and `model` metadata and require `providerID=openai`/`id=gpt-6-luna`. Abort if the child is Sol, inherits an unspecified default, or model metadata is absent. Only then give the pilot owned implementation paths and focused tests; expand gradually after integration. A direct CLI run with `--agent luna-model-probe` falls back because the agent is a **subagent**, so it is not proof of a child model. Effective child metadata remains **UNVERIFIED in this running session**; no worker has been assigned implementation work.

## Current model-gate checkpoint — 2026-09-23

- **PASS: GPT-6 Luna general child verified.** This checkpoint supersedes earlier unverified/failed-probe statements above. Before the probe, the working directory was exactly `C:\Github\9to1`, `main` HEAD was `0ff7867cf22fee6927b2608e26a616277c2e590b`, and `git status --short --branch` showed a clean `main...origin/main`. `.opencode/opencode.json` pins `build` to `openai/gpt-6-sol` and `general`/`explore` to `openai/gpt-6-luna`.
- Exactly one new, harmless read-only `general` child, `ses_f311d037effecHJ47KoVo4KRN3`, read `docs/9TO1_COMPLETION_STATE.md` and reported the authoritative path `C:\Github\9to1` and **Phase 01 — Boards, IN PROGRESS**. The desktop database session row records `parent_id=ses_f311d52d0ffe146GVuc5mYvGTY`, `directory=C:/Github/9to1`, `agent=general`, and `model={"id":"gpt-6-luna","providerID":"openai","variant":"default"}`. Both assistant message rows record `providerID=openai` and `modelID=gpt-6-luna`; `opencode.log` independently records `stream providerID=openai modelID=gpt-6-luna session.id=ses_f311d037effecHJ47KoVo4KRN3 ... agent=general mode=subagent`. The prior Sol-child failure remains historical evidence, not the outcome of this probe.
- **Model gate cleared only.** No framework implementation or surface-worker wave began; await the user's explicit continuation. Framework convergence and lane-isolation gates above remain separate.

## Framework continuation — shared theme semantics (2026-09-23)

- Following the user's continuation, corrected shared CUI theme application: `ApplyGlobalTheme(theme)` now updates the catalog's active theme and applies the requested palette; `ResolveThemeName` respects its supplied default; the loader snapshots the active theme when loading markup, including if the loader was constructed earlier. A Runtime regression verifies that a `Default` scope follows the current global theme while an explicit nested scope retains its own palette. `docs/architecture/cui.md` records the current contract and assigns one shared-framework owner; this is a scoped stabilization, **not** a declaration that all framework contracts are frozen.
- The GPT-6 Luna probe remains verified. No surface worker was launched. Cross-process theme preference persistence, live native Home/Boards interaction, Android device behavior, AXAML/HUI migration boundaries, DevTools live attachment and package-byte acceptance remain open; a full framework or surface-wave READY claim still requires direct evidence.
- Current checkout verification with task-local `TEMP`/`TMP`, `AVALONIA_TELEMETRY_OPTOUT=1`, invocation-only vendored CUI build-task DLL and `UsedAvaloniaProducts=`/`TreatWarningsAsErrors=false`: CUI Runtime Release **56/56**, Boards app Release **50/50**, native Home headless Release **1/1**. `powershell -NoProfile -ExecutionPolicy Bypass -File 'eng/9to1.ps1' -Action verify -Component core -Configuration Release` **PASS** (markup 4/4, Home 8/8, Spaces 12/12, preload metadata and release graph); it explicitly did not check package bytes and reported **8 CUI / 68 AXAML / 30 HUI** tracked markup files. These are bounded Windows build/headless gates, not native input, Android device or packaging acceptance.

## Framework continuation — shared preference consumer (2026-09-23)

- Found the authoritative preference writer at `9to1 Workspace/shared/src/Haven.Desktop/Services/UserPreferencesService.cs`: `havenUiThemeName` in the Haven data directory's `preferences.json`. Added a **read-only** CUI preference reader using the same data-directory convention (including `HAVEN_DATA_DIR`); no new settings file or writer. Native Home reads it when building its window; Boards reads it on startup, applies the shared theme palette and preserves its separate light/dark toggle. Home's headless test exercises a preference change between window builds. Boards tests verify Bubble survives light/dark switching. Unknown/missing/malformed values fall back to Glow.
- After this change, CUI Runtime Release **57/57**, native Home headless Release **1/1**, Boards app Release **51/51**. These prove startup/rebuild consumption in the tested hosts, **not** live cross-process updates or all-surface/global preference convergence. The remaining platform and migration gates above still apply; no surface worker was deployed.

## Framework continuation — loader isolation and live tree probe (2026-09-23)

- Reusing a CUI loader now clears prior document resources and live-binding registrations before loading another document; the regression confirms an authored resource cannot leak into a later document. The DevTools project previously could not build (ambiguous collection-copy overload and invalid read-only-list `IndexOf`); corrected those errors and added a read-only `CuiLiveTreeInspector` that observes native control hierarchy, bounds and accessibility name. Native Home exposes a bounded diagnostic capture from its current window; the headless route test searches its actual `nav-settings` control and checks observed bounds/name. **CUI Runtime Release 58/58 and Home headless Release 1/1** after these changes.
- This establishes a tested live-control tree bridge, **not** full DevTools diagnostic convergence: source locations, style rule provenance, binding/action/event traces, rendering timing and a physical GUI inspector remain unverified. No Linux or Android device acceptance was performed.
- Follow-up affected-host checks: Boards app Release **51/51** after loader reuse repair; Android app Release `--no-restore` build **PASS**, 0 warnings/errors. No Android device was connected through an available `adb` command, no WSL distribution is installed, and no staged `.deb` exists under `9to1 OS`; compilation and preload metadata do not satisfy device or package-byte acceptance.

## Framework continuation — live inspection contract (2026-09-23)

- Extended the Home-connected, read-only DevTools inspection path to carry authored `.cui` file/component/line provenance; binding expression, latest resolved/native value, missing/error/fallback trace; command identity, dispatcher wiring, handler registration and explicit host availability without dispatch; effective native visual properties, authored resource references, theme/radius resources, automation-peer role/name/states, bounds, opacity, visibility, clip and render scale. The loader records binding results during normal application/refresh; inspection does not invoke a command or re-evaluate a binding source. A faulted getter is recorded and uses its declared fallback. Home explicitly marks disconnected routes unavailable. Actual `Home.cui` route coverage checks Settings at line 20, active catalog binding, missing/faulted fallback on the same document, observed theme resources across Bubble/Retro window builds, and post-click status inspection. This is **headless native-host evidence**; frame timing, draw counts and physical compositor behavior are not invented.
- **Exact Release commands** (PowerShell in `C:\Github\9to1`):

  ```powershell
  $env:AVALONIA_TELEMETRY_OPTOUT='1'; $env:TEMP='C:\Users\Jacob\AppData\Local\Temp\opencode'; $env:TMP=$env:TEMP
  dotnet test 'framework/CUI/Runtime.Tests/CakeOS.Cui.Runtime.Tests.csproj' -c Release --no-restore -p:CuiBuildTasksLocation='C:\Github\9to1\framework\CUI\Compiler\CakeOS.Cui.Build.Tasks\bin\Release\netstandard2.0\CakeOS.Cui.Build.Tasks.dll' -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false
  dotnet test 'apps/Home/tests/AvaloniaHome.Tests/AvaloniaHome.Tests.csproj' -c Release --no-restore -p:CuiBuildTasksLocation='C:\Github\9to1\framework\CUI\Compiler\CakeOS.Cui.Build.Tasks\bin\Release\netstandard2.0\CakeOS.Cui.Build.Tasks.dll' -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false
  dotnet test '9to1 Workspace/Boards/app.tests/CakeOS.Apps.Boards.App.Tests.csproj' -c Release --no-restore -p:CuiBuildTasksLocation='C:\Github\9to1\framework\CUI\Compiler\CakeOS.Cui.Build.Tasks\bin\Release\netstandard2.0\CakeOS.Cui.Build.Tasks.dll' -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false
  dotnet build '9-1 OS (Android)/src/Haven.Android/Haven.Android.csproj' -c Release --no-restore -p:UsedAvaloniaProducts= -p:TreatWarningsAsErrors=false
  git diff --check
  ```

- Results: CUI Runtime **58/58**, Home headless **1/1**, Boards app **51/51**, Android Release build **PASS (0 warnings/errors)**, and `git diff --check` **PASS**. All are bounded host/build evidence. Native Windows input/DPI/accessibility/theme, Android device, Linux native rendering and `.deb` package-byte/install/launch remain **external acceptance gates**. Framework is **NOT READY**; no surface worker was launched.

## Surface-worker continuation — 2026-09-24

- Canonical checkout `C:\Github\9to1` is at `f54fb57257acbc06084d968673dac51d450367e1`. Six separate worktrees based on that revision delivered app-owned changes for Boards navigation state, Canvas gesture isolation, Spaces navigation serialization, Terminal session-replacement recovery, Browse background-tab selection and Data edit-address validation. Two additional isolated lanes delivered Pictures viewport overflow rejection and Home Dashboard in-flight disposal handling. Changes were reviewed and integrated into the canonical working tree without staging or committing; the separate worktrees retain their own edits.
- Canonical Release acceptance after integration: Boards app **52/52** using invocation-only `-p:RunAnalyzers=false` and the vendored build-task DLL; Canvas **5/5**; Spaces **13/13**; Browse **12/12**; Pictures **14/14**; Home Dashboard **10/10**; Terminal specs **PASS**; Data `--cell-contract-only` **PASS**. The default Boards Release build remains blocked by vendored Avalonia `AVADEV1001` (`Ref.CloneAs`); the full Data runtime harness needs LibreOffice `soffice`. These limitations are not passes for the blocked checks.
- All app lanes stayed inside their assigned surface paths. Physical Windows rendering/input/accessibility/DPI/theme, Android device, Linux native rendering, and `.deb` package-byte/install/launch acceptance remain open. Surface tests do not promote the framework or product to release readiness.
